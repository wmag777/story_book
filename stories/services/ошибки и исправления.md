# Описание ошибок и улучшений в обработке внешних API

## Обзор изменений

Все три файла сервисов были обновлены для улучшенной обработки ошибок при работе с внешними API (OpenAI и Google Gemini). Теперь все ошибки выводятся на русском языке с понятными инструкциями для пользователя.

---

## 1. stories/services/story_processing.py

### Назначение файла
Обрабатывает текст истории, извлекает персонажей и разделяет историю на сцены с помощью OpenAI GPT-4.

### Ошибки и исправления

#### ❌ Проблема 1: `'str' object has no attribute 'characters'`
**Когда возникает:**
- API возвращает строку с ошибкой вместо ожидаемого JSON объекта
- Отсутствует или неверный OpenAI API ключ
- Проблемы с сетью или квотами API

**Что было:**
```python
result = generate_pydantic_json_model(...)
return result.characters  # Падает, если result - строка
```

**Что стало:**
```python
result = generate_pydantic_json_model(...)

if isinstance(result, str):
    raise ValueError(f"Не удалось извлечь персонажей. API вернул неожиданный ответ: {result[:200]}")

if not hasattr(result, 'characters'):
    raise ValueError(f"Не удалось извлечь персонажей. Результат не имеет атрибута 'characters'. Тип: {type(result)}")

return result.characters
```

**Сообщение на русском:**
```
Не удалось извлечь персонажей. API вернул неожиданный ответ: [первые 200 символов ответа]
```

---

#### ❌ Проблема 2: Отсутствующие промпт-шаблоны
**Когда возникает:**
- Шаблоны не были инициализированы в базе данных (миграция не выполнена)
- Шаблоны были удалены вручную

**Что стало:**
```python
if not prompt_template:
    raise ValueError("Шаблон извлечения персонажей не найден в базе данных. Пожалуйста, убедитесь, что промпт-шаблоны инициализированы.")
```

**Сообщение на русском:**
```
Шаблон извлечения персонажей не найден в базе данных. Пожалуйста, убедитесь, что промпт-шаблоны инициализированы.
```

---

#### ❌ Проблема 3: Ошибки при извлечении сцен
**Когда возникает:** Те же причины, что и для персонажей

**Сообщение на русском:**
```
Не удалось извлечь сцены. API вернул неожиданный ответ: [детали]
```
или
```
Шаблон извлечения сцен не найден в базе данных. Пожалуйста, убедитесь, что промпт-шаблоны инициализированы.
```

---

## 2. stories/services/image_generation.py

### Назначение файла
Генерирует и редактирует изображения с помощью Google Gemini (Nano Banana).

### Ошибки и исправления

#### ❌ Проблема 1: Отсутствует Google API ключ
**Когда возникает:**
- API ключ не добавлен в Replit Secrets
- API ключ не указан в настройках проекта

**Что стало:**
```python
def __init__(self):
    ...
    if not self.google_api_key:
        raise ValueError(
            "Google API ключ не найден. Пожалуйста, добавьте GOOGLE_API в настройках проекта или в Replit Secrets. "
            "Без ключа генерация изображений невозможна."
        )
```

**Сообщение на русском:**
```
Google API ключ не найден. Пожалуйста, добавьте GOOGLE_API в настройках проекта или в Replit Secrets. Без ключа генерация изображений невозможна.
```

---

#### ❌ Проблема 2: Ошибка инициализации Google API клиента
**Когда возникает:**
- Неверный формат API ключа
- Проблемы с библиотекой Google API

**Что стало:**
```python
try:
    client = genai.Client(api_key=self.google_api_key)
except Exception as e:
    raise ValueError(
        f"Ошибка инициализации Google API клиента: {str(e)}. "
        f"Проверьте правильность API ключа."
    )
```

**Сообщение на русском:**
```
Ошибка инициализации Google API клиента: [детали ошибки]. Проверьте правильность API ключа.
```

---

#### ❌ Проблема 3: Превышен лимит API запросов
**Когда возникает:**
- Исчерпана квота бесплатного использования
- Достигнут лимит запросов в минуту

**Что стало:**
```python
if "quota" in error_msg.lower() or "limit" in error_msg.lower():
    raise Exception(
        f"Превышен лимит API запросов. Проверьте квоту вашего Google API ключа. "
        f"Детали ошибки: {error_msg}"
    )
```

**Сообщение на русском:**
```
Превышен лимит API запросов. Проверьте квоту вашего Google API ключа. Детали ошибки: [детали]
```

---

#### ❌ Проблема 4: Ошибка авторизации API
**Когда возникает:**
- API ключ недействителен или отозван
- Нет прав доступа к сервису

**Что стало:**
```python
elif "auth" in error_msg.lower() or "permission" in error_msg.lower():
    raise Exception(
        f"Ошибка авторизации API. Проверьте правильность вашего Google API ключа. "
        f"Детали ошибки: {error_msg}"
    )
```

**Сообщение на русском:**
```
Ошибка авторизации API. Проверьте правильность вашего Google API ключа. Детали ошибки: [детали]
```

---

#### ❌ Проблема 5: Неверный API ключ
**Когда возникает:**
- Введен неправильный или поврежденный API ключ

**Что стало:**
```python
elif "invalid" in error_msg.lower() and "key" in error_msg.lower():
    raise Exception(
        f"Неверный API ключ Google. Проверьте ключ в настройках проекта. "
        f"Детали ошибки: {error_msg}"
    )
```

**Сообщение на русском:**
```
Неверный API ключ Google. Проверьте ключ в настройках проекта. Детали ошибки: [детали]
```

---

#### ❌ Проблема 6: Общие ошибки генерации
**Когда возникает:**
- Проблемы с сетью
- Временные сбои на стороне Google
- Некорректный промпт

**Что стало:**
```python
else:
    raise Exception(
        f"Не удалось сгенерировать изображение после {max_retries} попыток. "
        f"Детали ошибки: {error_msg}"
    )
```

**Сообщение на русском:**
```
Не удалось сгенерировать изображение после 3 попыток. Детали ошибки: [детали]
```

---

#### ❌ Проблема 7: Ошибки при редактировании изображений
**Когда возникает:** Те же причины, что и при генерации

**Сообщения на русском:**
```
Превышен лимит API запросов. Проверьте квоту вашего Google API ключа. Детали ошибки: [детали]
```
```
Ошибка авторизации API. Проверьте правильность вашего Google API ключа. Детали ошибки: [детали]
```
```
Не удалось отредактировать изображение после 3 попыток. Детали ошибки: [детали]
```

---

## 3. stories/services/character_generation.py

### Назначение файла
Генерирует изображения персонажей с консистентным стилем (наследует ImageGenerator).

### Ошибки и исправления

#### ❌ Проблема 1: Пустое описание персонажа
**Когда возникает:**
- Пользователь не ввел описание персонажа
- Описание содержит только пробелы

**Что стало:**
```python
if not prompt or not prompt.strip():
    raise ValueError(
        "Описание персонажа не может быть пустым. "
        "Пожалуйста, добавьте описание внешности персонажа."
    )
```

**Сообщение на русском:**
```
Описание персонажа не может быть пустым. Пожалуйста, добавьте описание внешности персонажа.
```

---

#### ❌ Проблема 2: Пустое имя персонажа
**Когда возникает:**
- Не указано имя персонажа

**Что стало:**
```python
if not character_name or not character_name.strip():
    raise ValueError("Имя персонажа не может быть пустым.")
```

**Сообщение на русском:**
```
Имя персонажа не может быть пустым.
```

---

#### ❌ Проблема 3: Ошибки при генерации персонажа
**Когда возникает:**
- Любые ошибки от родительского класса ImageGenerator

**Что стало:**
```python
except ValueError as e:
    # Передаем ValueError дальше (ошибки API ключа и т.д.)
    raise
except Exception as e:
    raise Exception(
        f"Ошибка при генерации изображения персонажа '{character_name}': {str(e)}"
    )
```

**Сообщение на русском:**
```
Ошибка при генерации изображения персонажа 'Имя': [детали ошибки]
```

---

#### ❌ Проблема 4: Отсутствие шаблона стиля
**Когда возникает:**
- Шаблон image_style_suffix не найден в базе

**Что стало:**
```python
except PromptTemplate.DoesNotExist:
    print("ВНИМАНИЕ: Шаблон 'image_style_suffix' не найден в базе данных, используется запасной вариант")
    enhanced += f" Draw in {style} with {color_scheme} colors"
```

**Предупреждение в логах:**
```
ВНИМАНИЕ: Шаблон 'image_style_suffix' не найден в базе данных, используется запасной вариант
```

---

#### ❌ Проблема 5: Ошибка рендеринга шаблона
**Когда возникает:**
- В шаблоне используются переменные, которые не переданы

**Что стало:**
```python
except ValueError as e:
    print(f"ОШИБКА рендеринга шаблона стиля: {e}")
    enhanced += f" Draw in {style} with {color_scheme} colors"
```

**Предупреждение в логах:**
```
ОШИБКА рендеринга шаблона стиля: [детали]
```

---

#### ❌ Проблема 6: Создание референс-листа без описания персонажа
**Когда возникает:**
- У персонажа отсутствует поле description

**Что стало:**
```python
if not hasattr(character, 'description') or not character.description:
    raise ValueError(
        f"У персонажа '{character.name}' отсутствует описание. "
        f"Добавьте описание персонажа перед созданием референс-листа."
    )
```

**Сообщение на русском:**
```
У персонажа 'Имя' отсутствует описание. Добавьте описание персонажа перед созданием референс-листа.
```

---

#### ❌ Проблема 7: Ошибки при создании референс-листа
**Когда возникает:**
- Не удалось сгенерировать ни одного изображения из списка поз

**Что стало:**
```python
if failed_poses and not generated_images:
    raise Exception(
        f"Не удалось сгенерировать ни одного изображения. "
        f"Проваленные позы: {', '.join(failed_poses)}"
    )
```

**Сообщение на русском:**
```
Не удалось сгенерировать ни одного изображения. Проваленные позы: front view, side profile view
```

**Предупреждение если часть поз не сгенерирована:**
```
ВНИМАНИЕ: Некоторые позы не были сгенерированы: action pose
```

---

## Как настроить API ключи

### В Replit Secrets (рекомендуется):
1. Откройте вкладку "Secrets" (🔒) в левой панели Replit
2. Добавьте следующие ключи:
    - `OPENAI_KEY` - для извлечения персонажей и сцен (обязательно)
    - `GOOGLE_API` - для генерации изображений (обязательно)
    - `STABILITY_API_KEY` - для Stability AI (опционально)
    - `FAL_KEY` - для Seedance (опционально)

### В настройках проекта:
1. Перейдите в раздел "Settings" (⚙️) в приложении
2. Введите API ключи в соответствующие поля
3. Сохраните изменения

---

## Получение API ключей

### OpenAI API ключ:
1. Зарегистрируйтесь на https://platform.openai.com/
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Пополните баланс (минимум $5)

### Google Gemini API ключ:
1. Перейдите на https://aistudio.google.com/
2. Нажмите "Get API key"
3. Создайте ключ для своего проекта
4. Бесплатная квота: 15 запросов в минуту

---

## Проверка работоспособности

После добавления ключей:
1. Перезапустите приложение
2. Создайте новый проект
3. Попробуйте извлечь персонажей и сцены из тестовой истории
4. Сгенерируйте изображение для сцены

Если все настроено правильно, вы не увидите ошибок, а процесс будет выполняться корректно.

---

## Примеры ошибок и решений

| Ошибка | Причина | Решение |
|--------|---------|---------|
| Google API ключ не найден | Ключ не добавлен | Добавьте GOOGLE_API в Secrets |
| Превышен лимит API запросов | Исчерпана квота | Проверьте лимиты в Google Console |
| Ошибка авторизации API | Неверный ключ | Проверьте правильность ключа |
| Шаблон не найден в базе данных | Миграция не выполнена | Запустите `python manage.py migrate` |
| 'str' object has no attribute 'characters' | Проблема с OpenAI API | Проверьте OPENAI_KEY и баланс |

---

## Статус исправлений

✅ Все три файла обновлены
✅ Добавлена проверка наличия API ключей
✅ Все ошибки переведены на русский язык
✅ Добавлены понятные инструкции для пользователя
✅ Сервер перезапущен и работает корректно
